# Чеклист для публикации в Google Play

## 🔴 КРИТИЧЕСКИ ВАЖНО (нельзя публиковать без этого)

### 1. Изменить Application ID
**ПРОБЛЕМА:** Используется `com.example.linkedinapp` - это запрещено для production!
**РЕШЕНИЕ:** Изменить на уникальный ID, например:
- `com.yourcompany.linkjob`
- `ru.yourname.linkjob`
- `io.yourdomain.linkjob`

**⚠️ ВНИМАНИЕ:** После изменения нужно:
- Обновить `google-services.json` в Firebase Console
- Изменить package name во всех файлах
- Обновить Application ID в `build.gradle.kts`

### 2. Настроить Release Signing (Keystore)
**ПРОБЛЕМА:** Нет конфигурации для подписи release-сборки
**РЕШЕНИЕ:** 
1. Создать keystore:
```bash
keytool -genkey -v -keystore linkjob-release.keystore -alias linkjob -keyalg RSA -keysize 2048 -validity 10000
```

2. Добавить `keystore.properties` в корень проекта (не коммитить в Git!)

3. Обновить `build.gradle.kts` с signing config

**⚠️ ВАЖНО:** Сохранить keystore и пароли в БЕЗОПАСНОМ месте! Без них нельзя обновлять приложение!

### 3. Включить ProGuard/R8 для Release
**ПРОБЛЕМА:** `isMinifyEnabled = false` - приложение будет слишком большим
**РЕШЕНИЕ:** Включить минификацию в `build.gradle.kts`:
```kotlin
release {
    isMinifyEnabled = true
    isShrinkResources = true
    proguardFiles(...)
}
```

## 🟡 ВАЖНО (рекомендуется)

### 4. Обновить Target SDK
**Текущий:** `targetSdk = 34`
**Рекомендуется:** `targetSdk = 35` (если доступен)

### 5. Privacy Policy (Обязательно для Google Play Console)
**ПРОБЛЕМА:** Нет ссылки на Privacy Policy
**РЕШЕНИЕ:** 
1. Создать Privacy Policy страницу (можно использовать генераторы)
2. Разместить на веб-сайте (GitHub Pages, Firebase Hosting и т.д.)
3. Добавить ссылку в Google Play Console при создании приложения
4. (Опционально) Добавить ссылку в настройках приложения

### 6. App Icons
**Статус:** ✅ Есть адаптивные иконки
**Рекомендация:** Убедитесь, что все размеры качественные (512x512 для Play Store)

## 🟢 ЖЕЛАТЕЛЬНО (для оптимизации)

### 7. Version Management
**Текущий:** `versionCode = 1`, `versionName = "1.0"`
**Рекомендация:** Используйте семантическое версионирование

### 8. Store Listing (в Google Play Console)
Нужно подготовить:
- ✅ Скриншоты (минимум 2, максимум 8)
- ✅ Краткое описание (80 символов)
- ✅ Полное описание (4000 символов)
- ✅ Feature Graphic (1024 x 500 px)
- ✅ Иконка приложения (512 x 512 px)
- ✅ Категория и возрастной рейтинг

### 9. Тестирование
- ✅ Протестировать на разных устройствах
- ✅ Проверить работу с плохим интернетом
- ✅ Проверить работу уведомлений
- ✅ Проверить работу Firebase

### 10. Content Rating
Пройти опросник для определения возрастного рейтинга в Google Play Console

## ✅ Что уже готово

- ✅ Manifest настроен правильно
- ✅ Permissions обоснованы
- ✅ Firebase интегрирован
- ✅ Иконки приложения есть
- ✅ Версия приложения установлена
- ✅ Min SDK = 24 (поддерживает большинство устройств)

