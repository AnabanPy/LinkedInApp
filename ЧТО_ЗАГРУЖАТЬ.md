# Что загружать на GitHub

## ✅ ЗАГРУЖАТЬ (нужные файлы):

### Основные файлы проекта:
- `app/src/` - весь исходный код (НО БЕЗ папки `app/src/build/`)
- `app/build.gradle.kts`
- `app/proguard-rules.pro`
- `build.gradle.kts` (в корне)
- `settings.gradle.kts`
- `gradle.properties`
- `gradle/wrapper/` - вся папка с Gradle wrapper

### Документация:
- `README.md` (если есть)
- `CHANGE_APPLICATION_ID.md`
- `CHANGE_ID_STEP_BY_STEP.md`
- `GOOGLE_PLAY_CHECKLIST.md`
- `PRIVACY_POLICY_TEMPLATE.md`
- `QUICK_START.md`
- `README_PUBLICATION.md`
- `SETUP_SIGNING.md`
- `GITHUB_SETUP.md`
- `.gitignore`

### Скрипты (опционально):
- `build-release.bat`
- `build-release.sh`
- `create-keystore.bat`
- `create-keystore.sh`
- `keystore.properties.example`

### Firebase Functions (если используете):
- `functions/index.js`
- `functions/package.json`

---

## ❌ НЕ ЗАГРУЖАТЬ (игнорировать):

### Скомпилированные файлы:
- `app/build/` - ВСЯ папка (очень большая, не нужна)
- `build/` - папка в корне проекта
- `*.apk` - скомпилированные приложения
- `*.aab` - Android App Bundle
- `*.class` - скомпилированные Java классы
- `*.dex` - скомпилированные Android файлы

### Локальные настройки:
- `local.properties` - содержит пути к SDK (локальные)
- `keystore.properties` - содержит пароли и ключи (конфиденциально!)
- `*.keystore` - файлы ключей подписи
- `*.jks` - файлы ключей подписи
- `linkjob-release.keystore` - ключ подписи

### Конфигурация сервисов:
- `app/google-services.json` - конфигурация Firebase (может содержать ключи)

### Временные файлы:
- `.gradle/` - кэш Gradle
- `.idea/` - настройки Android Studio (локальные)
- `*.iml` - файлы модулей IntelliJ

---

## 💡 Совет:

Если не уверены, что загружать - просто загрузите:
1. Всю папку `app/src/` (кроме `app/src/build/`)
2. Все `.gradle.kts` и `.properties` файлы
3. Папку `gradle/wrapper/`
4. Файл `.gitignore`
5. Документацию (`.md` файлы)

**НЕ загружайте папку `build/` - она очень большая и не нужна!**


