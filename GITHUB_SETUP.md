# Инструкция по переносу проекта на GitHub

## 🚀 Самый простой способ: Загрузка через сайт GitHub

**Да, можно загрузить проект прямо через сайт!** Это самый простой способ, если не хотите использовать командную строку.

### Шаг 1: Создайте репозиторий на GitHub

1. Войдите в свой аккаунт на https://github.com
2. Нажмите кнопку **"+"** в правом верхнем углу
3. Выберите **"New repository"**
4. Введите название репозитория (например: `LinkedInApp`)
5. Выберите **Public** (чтобы другие могли посмотреть и установить)
6. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
7. Нажмите **"Create repository"**

### Шаг 2: Загрузите файлы через веб-интерфейс

После создания репозитория GitHub покажет страницу с инструкциями. Вот где найти кнопку загрузки:

**Способ 1 (самый простой):**
- На странице репозитория найдите текст **"uploading an existing file"** (это синяя ссылка в разделе "Quick setup")
- **Кликните на эту ссылку** - она откроет страницу загрузки файлов

**Способ 2:**
- В правом верхнем углу страницы репозитория найдите зеленую кнопку **"Code"** (или **"Add file"**)
- Нажмите на неё и выберите **"Upload files"**

**Способ 3:**
- Просто начните создавать новый файл, нажав на кнопку **"creating a new file"** (тоже синяя ссылка)
- Затем переключитесь на загрузку файлов

**После того как откроется страница загрузки:**

1. Откройте папку вашего проекта: `C:\Users\dokto\AndroidStudioProjects\LinkedInApp`
2. **Выберите ВСЕ файлы и папки** (кроме папки `build`, она не нужна - она в .gitignore)
3. **Перетащите их** в окно браузера (в область с пунктирной рамкой) или нажмите **"choose your files"**
4. Внизу страницы введите сообщение коммита: `Initial commit`
5. Нажмите зеленую кнопку **"Commit changes"**

### ⚠️ Важно при загрузке через сайт:

- **НЕ загружайте папку `build/`** - она содержит скомпилированные файлы и очень большая
- **НЕ загружайте `local.properties`** - это локальные настройки
- **НЕ загружайте `keystore.properties`** и файлы `.keystore` или `.jks` - это ключи подписи
- **НЕ загружайте `google-services.json`** - это конфиденциальная конфигурация

### ✅ Что нужно загрузить:

📋 **Подробный список файлов смотрите в файле `ЧТО_ЗАГРУЖАТЬ.md`**

Кратко:
- Все `.kt` файлы (исходный код)
- Все `.xml` файлы (макеты, ресурсы)
- `build.gradle.kts` файлы
- `settings.gradle.kts`
- `gradle.properties`
- Папку `gradle/wrapper/`
- Файлы `.gitignore`, `README.md` и другие документационные файлы
- Папку `app/src/` (но не `app/build/`)

### После загрузки:

Ваш проект будет доступен по адресу: `https://github.com/ВАШ_USERNAME/LinkedInApp`

Другие смогут:
- Просматривать код
- Клонировать репозиторий
- Устанавливать приложение (если вы добавите APK файл в раздел Releases)

---

## 📝 Альтернативный способ: Через командную строку (для продвинутых)

Если хотите использовать Git команды:

## Шаг 1: Установка Git (если еще не установлен)

1. Скачайте Git для Windows: https://git-scm.com/download/win
2. Установите Git, следуя инструкциям установщика
3. Перезапустите терминал/командную строку после установки

## Шаг 2: Проверка установки Git

Откройте PowerShell или командную строку и выполните:
```bash
git --version
```

Если Git установлен, вы увидите версию.

## Шаг 3: Настройка Git (первый раз)

Если вы используете Git впервые, настройте имя и email:

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

## Шаг 4: Инициализация репозитория

Перейдите в папку проекта и инициализируйте Git:

```bash
cd C:\Users\dokto\AndroidStudioProjects\LinkedInApp
git init
```

## Шаг 5: Добавление файлов в репозиторий

```bash
git add .
```

## Шаг 6: Создание первого коммита

```bash
git commit -m "Initial commit"
```

## Шаг 7: Создание репозитория на GitHub

1. Войдите в свой аккаунт на https://github.com
2. Нажмите кнопку "+" в правом верхнем углу
3. Выберите "New repository"
4. Введите название репозитория (например: `LinkedInApp`)
5. Выберите Public или Private
6. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
7. Нажмите "Create repository"

## Шаг 8: Подключение локального репозитория к GitHub

После создания репозитория GitHub покажет вам команды. Выполните:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/LinkedInApp.git
```

Замените `ВАШ_USERNAME` на ваш GitHub username.

## Шаг 9: Отправка кода на GitHub

```bash
git branch -M main
git push -u origin main
```

Если GitHub попросит авторизацию:
- Используйте Personal Access Token вместо пароля
- Создайте токен: Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выберите права: `repo` (полный доступ к репозиториям)

## Альтернативный способ: Через GitHub Desktop

Если командная строка кажется сложной, используйте GitHub Desktop:

1. Скачайте: https://desktop.github.com/
2. Установите и войдите в свой GitHub аккаунт
3. File → Add Local Repository → выберите папку LinkedInApp
4. Нажмите "Publish repository" в правом верхнем углу

## Проверка

После успешной загрузки откройте ваш репозиторий на GitHub:
`https://github.com/ВАШ_USERNAME/LinkedInApp`

---

## Важные замечания

✅ Файл `.gitignore` уже настроен и исключает:
- Скомпилированные файлы (build/, *.apk, *.class)
- Локальные настройки (local.properties)
- Ключи подписи (keystore.properties, *.jks)
- Google Services конфигурацию (google-services.json)

⚠️ **ВНИМАНИЕ**: Убедитесь, что конфиденциальные данные (API ключи, пароли) не попали в репозиторий!

